{{- $namespace := default .Release.Namespace .Values.namespace -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.paymentService.configMap.name }}
  namespace: {{ $namespace }}
data:
{{ toYaml .Values.paymentService.configMap.data | nindent 2 }}
