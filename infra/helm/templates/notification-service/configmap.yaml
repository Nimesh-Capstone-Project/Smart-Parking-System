{{- $namespace := default .Release.Namespace .Values.namespace -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.notificationService.configMap.name }}
  namespace: {{ $namespace }}
data:
{{ toYaml .Values.notificationService.configMap.data | nindent 2 }}
