apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-deploy
  namespace: sps-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sps-booking
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: sps-booking
    spec:
      containers:
        - name: booking-container
          image: nimeshsv814/booking-service:v1.0.0
          ports:
            - containerPort: 4003
          envFrom:
            - configMapRef:
                name: auth-configmap
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"